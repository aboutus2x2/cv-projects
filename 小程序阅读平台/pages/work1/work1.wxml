<view class="box" style="background-color: {{color}}"></view>
<button bindtap="onToggle">开关</button>

<view class="user" wx:for="{{users}}" wx:key="id">
  <view>姓名: {{item.name}}</view>
  <button bindtap="remove" data-id="{{item.id}}">删除</button>
</view>